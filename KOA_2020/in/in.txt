include "DateTime.lib"
string prototype GetDate();
string prototype GetTime();
uint prototype cWrite();
uint prototype cWriteLine();
string prototype UintToChar();
uint prototype GetRandom();

string function GetCurrentTime()
{
    string time = "Date: " + GetDate() + ". Time: " + GetTime() + ".";
    return time;
}

main
{
    cWriteLine(GetCurrentTime());
    cWriteLine("-----------------------------------------------");
    string resultString;
    bool xIsTheBiggest;
    bool yIsTheBiggest;
    bool zIsTheBiggest;
    uint x = GetRandom(0, 2077);
    uint y = GetRandom(0, 2077);
    uint z = GetRandom(0, 2077;
    string startInfo = "Найти максимальное среди чисел: " + UintToChar(x) + ", " + UintToChar(x) + ", "  + UintToChar(x);
    cWriteLine(startInfo);
    if(x > y)
    {
       if(x > z)
       {
            xIsTheBiggest = true;
       }
       else
       {
            zIsTheBiggest = true;
       }
    }
    else
    {
        if(y > z)
        {
            yIsTheBiggest = true;
        }
        else
        {
            zIsTheBiggest = true;
        }
    }

    if(xIsTheBiggest) { resultString =  UintToChar(x) + "Самое большое число!" }
    if(yIsTheBiggest) { resultString =  UintToChar(y) + "Самое большое число!" }
    if(zIsTheBiggest) { resultString =  UintToChar(z) + "Самое большое число!" }

    cWriteLine(resultString);

 	return 0;
}